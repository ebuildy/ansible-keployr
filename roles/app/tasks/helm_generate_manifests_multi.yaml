---

-
  name: Explode all.yaml
  loop: "{{ lookup('file', keployr_build_helm_dir ~ '/all.yaml') | from_yaml_all }}"
  copy:
    dest: "{{ keployr_build_manifests_dir }}/{{ '%s-%s.yaml' % (item.metadata.name | lower, item.kind | lower) }}"
    content: "{{ item | to_nice_yaml }}"